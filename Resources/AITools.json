{
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "tool_getProducts",
        "description": "Search for products in the database by ID, reference code, name, or price range. Use wildcards (@) for partial matching. Returns matching products with their ID, Reference, Name, and Sale_Price. Results can be sorted by any field and limited to a maximum number. Returns a counts object with total, totalFiltered, and totalSent to indicate if results were truncated. If ID is provided, it performs a direct lookup before applying other filters.",
        "parameters": {
          "type": "object",
          "properties": {
            "ID": {
              "type": "string",
              "description": "Product ID for direct lookup. Use 'any' or omit to search by other criteria. When provided, searches for this specific product first, then applies other filters."
            },
            "Reference": {
              "type": "string",
              "description": "Product reference code. Supports wildcard matching with @. Example: 'PRD@' matches all references starting with PRD. Defaults to '@' (all) if not provided."
            },
            "Name": {
              "type": "string",
              "description": "Product name. Supports wildcard matching with @. Example: '@laptop@' matches any product name containing 'laptop'. Defaults to '@' (all) if not provided."
            },
            "Sale_Price": {
              "type": "object",
              "properties": {
                "min": {
                  "type": "number",
                  "description": "Minimum sale price (inclusive). Defaults to 0 if not provided."
                },
                "max": {
                  "type": "number",
                  "description": "Maximum sale price (inclusive). Defaults to 999999 if not provided."
                }
              }
            },
            "orderBy": {
              "type": "object",
              "properties": {
                "field": {
                  "type": "string",
                  "description": "Field name to sort by. Valid values: ID, Reference, Name, Sale_Price. Defaults to 'Name' if not provided."
                },
                "order": {
                  "type": "string",
                  "enum": ["asc", "desc"],
                  "description": "Sort order. Must be either 'asc' (ascending) or 'desc' (descending). Defaults to 'asc' if not provided."
                }
              }
            },
            "top": {
              "type": "number",
              "description": "Maximum number of results to return. If not provided, defaults to the system's maxTop limit. Use this to limit large result sets. The response will include counts object showing if results were truncated."
            }
          },
          "minProperties": 1,
          "strict": true,
          "additionalProperties": false
        },
        "x-examples": [
          {
            "description": "Search for products with names containing 'computer' in the price range 500-2000",
            "value": {
              "Name": "@computer@",
              "Sale_Price": {
                "min": 500,
                "max": 2000
              }
            }
          },
          {
            "description": "Search for all products with reference starting with 'ELEC'",
            "value": {
              "Reference": "ELEC@"
            }
          },
          {
            "description": "Search for products priced under 100",
            "value": {
              "Reference": "",
              "Name": "",
              "Sale_Price": {
                "min": 0,
                "max": 100
              }
            }
          }
        ]
      }
    },
    {
      "type": "function",
      "function": {
        "name": "tool_getClients",
        "description": "Search for clients in the database by ID, name, contact, or total sales range. Use wildcards (@) for partial matching. Returns matching clients with their ID, Name, Contact, City, State, Country, Discount_Rate, Total_Sales, and Comments. Results can be sorted by any field and limited to a maximum number. Returns a counts object with total, totalFiltered, and totalSent to indicate if results were truncated. If ID is provided, it performs a direct lookup before applying other filters.",
        "parameters": {
          "type": "object",
          "properties": {
            "ID": {
              "type": "string",
              "description": "Client ID for direct lookup. Use 'any' or omit to search by other criteria. When provided, searches for this specific client first, then applies other filters."
            },
            "Name": {
              "type": "string",
              "description": "Client name. Supports wildcard matching with @. Example: '@Smith@' matches any client name containing 'Smith'. Defaults to '@' (all) if not provided."
            },
            "Contact": {
              "type": "string",
              "description": "Contact person name. Supports wildcard matching with @. Example: '@John@' matches any contact name containing 'John'. Defaults to '@' (all) if not provided."
            },
            "Total_Sales": {
              "type": "object",
              "properties": {
                "min": {
                  "type": "number",
                  "description": "Minimum total sales amount (inclusive). Defaults to 0 if not provided."
                },
                "max": {
                  "type": "number",
                  "description": "Maximum total sales amount (inclusive). Defaults to 9999999 if not provided."
                }
              }
            },
            "orderBy": {
              "type": "object",
              "properties": {
                "field": {
                  "type": "string",
                  "description": "Field name to sort by. Valid values: ID, Name, Contact, City, State, Country, Discount_Rate, Total_Sales, Comments. Defaults to 'Name' if not provided."
                },
                "order": {
                  "type": "string",
                  "enum": ["asc", "desc"],
                  "description": "Sort order. Must be either 'asc' (ascending) or 'desc' (descending). Defaults to 'asc' if not provided."
                }
              }
            },
            "top": {
              "type": "number",
              "description": "Maximum number of results to return. If not provided, defaults to the system's maxTop limit. Use this to limit large result sets. The response will include counts object showing if results were truncated."
            }
          },
          "minProperties": 1,
          "strict": true,
          "additionalProperties": false
        },
        "x-examples": [
          {
            "description": "Search for clients with names containing 'Tech' and total sales over 10000",
            "value": {
              "Name": "@Tech@",
              "Total_Sales": {
                "min": 10000,
                "max": 9999999
              }
            }
          },
          {
            "description": "Search for clients with contact person containing 'John'",
            "value": {
              "Contact": "@John@"
            }
          },
          {
            "description": "Search for clients with total sales between 5000 and 20000",
            "value": {
              "Name": "@",
              "Total_Sales": {
                "min": 5000,
                "max": 20000
              }
            }
          }
        ]
      }
    },
    {
      "type": "function",
      "function": {
        "name": "tool_getInvoices",
        "description": "Search for invoices in the database by invoice number, client ID, or total amount range. Use wildcards (@) for partial matching. Returns matching invoices with their ID, Invoice_Number, Client_ID, Creation_Date, Paid status, Payment_Date, and Total. Results can be sorted by any field and limited to a maximum number. Returns a counts object with total, totalFiltered, and totalSent to indicate if results were truncated. If Client_ID is provided and not 'any', it filters invoices for that specific client.",
        "parameters": {
          "type": "object",
          "properties": {
            "Invoice_Number": {
              "type": "string",
              "description": "Invoice number. Supports wildcard matching with @. Example: 'INV@' matches all invoice numbers starting with INV. Defaults to '@' (all) if not provided."
            },
            "Client_ID": {
              "type": "string",
              "description": "Client ID to filter invoices. Use 'any' or omit to search invoices from all clients. When provided with a specific ID, only returns invoices for that client."
            },
            "Total": {
              "type": "object",
              "properties": {
                "min": {
                  "type": "number",
                  "description": "Minimum invoice total amount (inclusive). Defaults to 0 if not provided."
                },
                "max": {
                  "type": "number",
                  "description": "Maximum invoice total amount (inclusive). Defaults to 9999999 if not provided."
                }
              }
            },
            "orderBy": {
              "type": "object",
              "properties": {
                "field": {
                  "type": "string",
                  "description": "Field name to sort by. Valid values: ID, Invoice_Number, Client_ID, Creation_Date, Paid, Payment_Date, Total. Defaults to 'Total' if not provided."
                },
                "order": {
                  "type": "string",
                  "enum": ["asc", "desc"],
                  "description": "Sort order. Must be either 'asc' (ascending) or 'desc' (descending). Defaults to 'desc' if not provided."
                }
              }
            },
            "top": {
              "type": "number",
              "description": "Maximum number of results to return. If not provided, defaults to the system's maxTop limit. Use this to limit large result sets. The response will include counts object showing if results were truncated."
            }
          },
          "minProperties": 1,
          "strict": true,
          "additionalProperties": false
        },
        "x-examples": [
          {
            "description": "Search for invoices with total amount over 1000",
            "value": {
              "Invoice_Number": "@",
              "Total": {
                "min": 1000,
                "max": 9999999
              }
            }
          },
          {
            "description": "Search for invoices from a specific client",
            "value": {
              "Client_ID": "CLT123"
            }
          },
          {
            "description": "Search for invoices with number starting with 'INV2024' and total between 500 and 5000",
            "value": {
              "Invoice_Number": "INV2024@",
              "Total": {
                "min": 500,
                "max": 5000
              }
            }
          }
        ]
      }
    },
    {
      "type": "function",
      "function": {
        "name": "tool_getInvoiceLines",
        "description": "Search for invoice lines in the database by invoice number, invoice ID, client ID, product ID, quantity range, or total amount range. Use wildcards (@) for partial matching on text fields. Returns detailed invoice line items with ID, Line_Number, Invoice.Invoice_Number, Invoice.Client_ID, Total, Quantity, Product.Reference, and Product.Name. Results can be sorted by any field and limited to a maximum number. Returns a counts object with total, totalFiltered, and totalSent to indicate if results were truncated. Useful for analyzing product sales, client purchases, or specific invoice details.",
        "parameters": {
          "type": "object",
          "properties": {
            "Invoice_Number": {
              "type": "string",
              "description": "Invoice number. Supports wildcard matching with @. Example: 'INV@' matches all invoice numbers starting with INV. Defaults to '@' (all) if not provided."
            },
            "Invoice_ID": {
              "type": "string",
              "description": "Invoice ID for direct lookup. Use 'any' or omit to search across all invoices. When provided, returns only lines from this specific invoice."
            },
            "Client_ID": {
              "type": "string",
              "description": "Client ID to filter invoice lines. Use 'any' or omit to search across all clients. When provided, returns only lines from invoices belonging to this client."
            },
            "Product_ID": {
              "type": "string",
              "description": "Product ID to filter invoice lines. Use 'any' or omit to search across all products. When provided, returns only lines containing this specific product."
            },
            "Quantity": {
              "type": "object",
              "properties": {
                "min": {
                  "type": "number",
                  "description": "Minimum quantity (inclusive). Defaults to 0 if not provided."
                },
                "max": {
                  "type": "number",
                  "description": "Maximum quantity (inclusive). Defaults to 9999999 if not provided."
                }
              }
            },
            "Total": {
              "type": "object",
              "properties": {
                "min": {
                  "type": "number",
                  "description": "Minimum line total amount (inclusive). Defaults to 0 if not provided."
                },
                "max": {
                  "type": "number",
                  "description": "Maximum line total amount (inclusive). Defaults to 9999999 if not provided."
                }
              }
            },
            "orderBy": {
              "type": "object",
              "properties": {
                "field": {
                  "type": "string",
                  "description": "Field name to sort by. Valid values: ID, Line_Number, Total, Quantity. Defaults to 'Total' if not provided."
                },
                "order": {
                  "type": "string",
                  "enum": ["asc", "desc"],
                  "description": "Sort order. Must be either 'asc' (ascending) or 'desc' (descending). Defaults to 'desc' if not provided."
                }
              }
            },
            "top": {
              "type": "number",
              "description": "Maximum number of results to return. If not provided, defaults to the system's maxTop limit. Use this to limit large result sets. The response will include counts object showing if results were truncated."
            }
          },
          "minProperties": 1,
          "strict": true,
          "additionalProperties": false
        },
        "x-examples": [
          {
            "description": "Search for all invoice lines for a specific product",
            "value": {
              "Product_ID": "PRD123"
            }
          },
          {
            "description": "Search for invoice lines from a specific client with quantity over 10",
            "value": {
              "Client_ID": "CLT456",
              "Quantity": {
                "min": 10,
                "max": 9999999
              }
            }
          },
          {
            "description": "Search for high-value invoice lines (total over 1000) from invoices starting with 'INV2024'",
            "value": {
              "Invoice_Number": "INV2024@",
              "Total": {
                "min": 1000,
                "max": 9999999
              }
            }
          }
        ]
      }
    }
  ]
}
